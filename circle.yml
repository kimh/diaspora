machine:
  ruby:
    version: 2.3.4

dependencies:
  override:
    - gem install bundler
    - RAILS_ENV=production bin/bundle install --no-deployment --without test development --with postgresql --path vendor/bundle

database:
  override:
    - RAILS_ENV=production bin/rake db:create db:migrate

test:
  override:
    - exit 0

